import 'package:flutter/material.dart';

class TodoAdd extends StatelessWidget {
  const TodoAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this is to add'),
    );
  }
}
